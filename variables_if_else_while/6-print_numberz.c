#include <stdio.h>

/**
*main - printing all single digit numbers
* without using variables of type char*
*Return: always 0 (success)
*/

int main(void)

{

	int n;

for (n = 0; n <= 9; n++)
	putchar(n + '0');
	putchar(n + '0');

return (0);

}
